---
created: 2026-06-24
---

# Tests for JSON output mode

Add unit/integration tests covering: JSON output for list, status, gc, ship; structured JSON errors; CLI flag precedence over config; `output_format` TOML parsing. Use `MockSsh` as per existing test patterns.
